# September run bundle — START HERE

Cut from the repo by `make_run_bundle.py` at commit `4fe8246` and verified from the extract.
**The runbook (`WWTC_test_run_starter.md`, Runbook 8.12) governs the whole run.** This page is not a
procedure and adds nothing to it.

## The opening prompt — paste this to begin

> This is a RUN / simulation session, governed by the runbook `WWTC_test_run_starter.md`:
> a SEPTEMBER PLANNING RUN for the January 2027 calendar. Last year's field is the stand-in;
> my real venues, dates and rules for next January go in on the Setup console; the divisions
> I am adding enter at Step 1.5. No commits, no pushes, no repo edits — report findings back
> to me at the end. Start a WWTC run.

## What is in the bundle

- the 23 modules of the runbook's Setup list, and the two console templates
- the data set under `data/wwtc-2026/` — four player lists, two draws files
- next January's own answers under `data/wwtc-2027/` — the clubs, days, rules and added
  divisions the Setup console opens on at Step 1, verified from this extract
- the runbook itself

Nothing extra is attached for a September run. The divisions the director is adding are typed
on the Setup console and ride the one block he couriers back; Step 1.5 turns them into a field.
